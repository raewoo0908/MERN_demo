PORT=4000
CLIENT_URL=http://localhost:5173

# In dev, point to local MongoDB (started via `docker compose up -d`).
# In prod, point to MongoDB Atlas (aggregated collections only).
MONGO_URI=mongodb://localhost:27017/ddareungi
